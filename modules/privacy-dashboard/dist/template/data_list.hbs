{{#each this}}
    <div class="item">
        <div class= "{{#if send}}value allowed{{else}}value blocked{{/if}}" title="{{val}}">
            {{val}}
        </div>
        <div class="legend">
            {{name}} {{#if des}}({{des}}){{/if}}
        </div>
    </div>
{{/each}}

