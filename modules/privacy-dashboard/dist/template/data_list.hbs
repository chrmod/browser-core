{{#each this}}
    <div class="item">
        <div class= "{{#if send}}value allowed{{else}}value blocked{{/if}}">
            {{val}}
        </div>
        <div class="legend">
            {{name}} {{#if des}}({{des}}){{/if}}
        </div>
    </div>
{{/each}}

