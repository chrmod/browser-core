<!-- footer-message.tpl -->
<!-- <div class="cqz-message-bar">
  <div class="cqz-message {{type}}">
      {{ simple_message }}
      <b>
        {{message}}

        {{#each messages}}
          {{#if this.correctBack}}
            <i>{{this.correctBack}}</i>
         {{else}}
           {{this.correct}}
         {{/if}}
        {{/each}}
      </b>
  </div>
  	<table class="footer-message-action" cellpadding="0" cellspacing="0" cliqz-action="footer-message-action" cliqz-telemetry="{{telemetry}}">
  		<tr>
  			{{#each options}}
   			 	<td><span class="cqz-btn cqz-btn-{{ state }}" state="{{ action }}" {{#if pref }} pref="{{../pref}}" {{/if}}  {{#if prefVal }} prefVal="{{prefVal}}" {{/if}} >{{ text }}</span></td>
    			{{/each}}
		</tr>
	</table>
</div> -->
<!-- end footer-message.tpl -->